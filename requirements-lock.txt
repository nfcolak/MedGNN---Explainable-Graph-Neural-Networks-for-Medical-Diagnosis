# Locked dependency set for the working local environment used for this project.
# Python used locally: 3.9.6
#
# This is intentionally a minimal project lock, not a full `pip freeze` dump.
# The global environment contains many unrelated packages; only packages used by
# the MIMIC ED graph pipeline, ProtGNN, GraphXAI explainers, and summary export
# are pinned here.

torch==2.8.0
torch-geometric==2.6.1

numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
scikit-learn==1.6.1

networkx==3.2.1
matplotlib==3.9.4
tqdm==4.67.3

optuna==4.8.0
xgboost==2.1.4
rdkit==2025.9.2

# Used for native .xlsx output. The project has a fallback writer if this is
# missing, but installing it is recommended.
openpyxl==3.1.5
